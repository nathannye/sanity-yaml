defineField({
    name: '{{name}}',
    type: 'reference',
    to: [{{#each to}}
        { type: '{{this.type}}' }{{#unless @last}},{{/unless}}
    {{/each}}],
    {{> sanity-validation validation=validation}}
}),